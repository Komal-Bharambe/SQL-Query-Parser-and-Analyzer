# SQL-Query-Parser-and-Analyzer
A simple and efficient SQL Parsing application built using the mvl-sql-parser library. This project demonstrates how to parse SQL queries, extract components like tables, columns, and clauses, and convert SQL into structured JSON objects for analysis or transformation.
